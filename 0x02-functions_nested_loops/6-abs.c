#include "main.h"
/**
 * _abs - computes the absolute value of an interger
 * @t: the integer to be computed
 * Return: the abolsute value of t
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
